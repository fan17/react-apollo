import React from 'react';
import { NavLink } from 'react-router-dom';

const SimpleHeader = () => {
    return (
        <header>
            <NavLink to="/">🚀</NavLink>
        </header>
    );
};

export default SimpleHeader;
